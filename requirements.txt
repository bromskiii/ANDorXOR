# Core data + I/O
pandas>=1.5
numpy>=1.24
openpyxl>=3.1   # required for pandas.read_excel with .xlsx

# HTTP / XML
requests>=2.30

# Environment
python-dotenv>=1.0

# Google Gemini client used in your scripts
google-generativeai>=0.4.0

# Transformers-based image classifier (pipeline)
transformers>=4.30
torch>=2.0
torchvision>=0.15
pillow>=9.0

# Utility (mime types, base64, logging, xml.etree are stdlib so not listed)

